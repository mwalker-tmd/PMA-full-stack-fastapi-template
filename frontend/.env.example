# Frontend Environment Variables
# Copy this file to .env and adjust values as needed for your environment
#
# Note: These variables are bundled into the client-side JavaScript and are visible to users.
# NEVER put secrets or sensitive data in frontend environment variables.

# API Base URL
# For local development, use: http://localhost:8000
# For production, use your production API URL (e.g., https://api.yourdomain.com)
VITE_API_URL=http://localhost:8000
